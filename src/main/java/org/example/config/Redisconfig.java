package org.example.config;

import org.springframework.context.annotation.Configuration;

/**
 * Redis configuration skeleton. Configure Redis connection in application.properties
 * and let Spring Boot autoconfigure RedisConnectionFactory and RedisTemplate.
 */
@Configuration
public class Redisconfig {
	// Minimal placeholder for Redis-specific beans (CacheManager, custom serializers, etc.)
}
